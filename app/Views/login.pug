include mixins/base
html
  head
    title=title
    link(rel="stylesheet" href="/stylesheets/style.css")
  body
    include mixins/header
    #main-container.container.p-5
      +flash
      h1 Connexion
      p Connectez vous de façon sécuritaire pour gérer vos mots de passe
      .text-center.mx-auto.col-6
        form.text-center.mx-auto(action="/account/login" method="POST")
          .form-group
            input.form-control(type="text" name="username" placeholder="Nom d'utilisateur")
          .form-group
            input.form-control(type="password" name="password" placeholder="Mot de passe")
          .form-group.mb-1
            input.form-check-input(type="checkbox" name="rememberMe" id="remember")
            label(for="remember") Se souvenir de moi
          button.btn.btn-dark(type="submit") Se connecter
      .text-center.mx-auto
        p Pas encore de compte? &nbsp;
          a(href="/signUp") Inscrivez-vous ici
    include mixins/footer
